apiVersion: v1
kind: Service
metadata:
  name: stock-management-service
  namespace: wms-system
  labels:
    app: stock-management-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: stock-management-service

