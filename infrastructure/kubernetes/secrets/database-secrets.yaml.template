apiVersion: v1
kind: Secret
metadata:
  name: wms-database-secrets
  namespace: wms-system
type: Opaque
stringData:
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "CHANGE_ME"
  # Base64 encode values before applying:
  # echo -n 'your-password' | base64

